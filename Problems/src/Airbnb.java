import java.util.Map;

public class Airbnb {
   //replace S with keys in map.
   //I went to S F and stayed at airbnb
   // I went to city and stayed at business
   // map S - a, F - b. SF - city, airbinb - bussiness

   private static String convert(String line, Map map) {
      if(map == null || map.size() == 0 || line == null || line.length() == 0)
         return line;
      StringBuilder sb = new StringBuilder(line);
      for(int start = 0; start < sb.length(); start++) {

      }
      return line;
   }

   public static void main(String[] args) {

   }
}
